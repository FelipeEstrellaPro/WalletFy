targets:
  $default:
    builders:
      drift_dev:
        options:
          sql:
            dialect: sqlite
          store_date_time_values_as_text: true
          generate_manager: true
          skip_verification_code: true
